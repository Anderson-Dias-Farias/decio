version: "3.9"

services:
  meu_app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: decio-app
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./app:/app
